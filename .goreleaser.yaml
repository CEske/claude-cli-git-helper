version: 2
builds:
  - binary: claude-git-helper
    goos:
      - windows
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - formats: [zip]
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
